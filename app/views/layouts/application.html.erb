<!DOCTYPE html>
<html>
<head>
  <title>Blocipedia</title>
  <!--add url for google font merriweather here get from google.com/fonts (maybe) -->
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container" id="primary-menu">
  <ul class="nav nav-tabs">
    <!--need to create class for current page then add to each li-->
    <!--<li><%= link_to "Home", welcome_index_path,:class => current_page?(welcome_index_path) ? 'current':'' %></li>-->
    <li><%= link_to "About", welcome_about_path %></li>

    <div class="pull-right user-info">
      <% if current_user %>
          Hello <%= current_user.email %>! <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
      <% else %>
          <%= link_to "Sign In", new_user_session_path %> or
          <%= link_to "Sign Up", new_user_registration_path %>
      <% end %>
    </div>

  </ul>
</div>
<div class="container">
  <%= yield %>
</div>

</body>
</html>
