%h1= @wiki.title
%p= raw @wiki.markdown_body

/ need to finish this later and add policies
/- if current_user.can_edit?
= link_to "Update", edit_wiki_path

/= link_to "New", _path

/ need to work on this the path is not named or created yet
/- if current_user.owns?(@wiki)
/  = link_to "Add Collaborators", my_wiki_collaborators_path(@wiki)

